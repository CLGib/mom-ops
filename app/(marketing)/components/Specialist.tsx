export default function Specialist() {
  return (
    <section id="specialist" className="section">
      <div className="container">
        <h2 className="section-title">The things that never end? Set them and forget them.</h2>
        <p className="section-lead">
          The mental load isn&apos;t one big task — it&apos;s the same things,
          every week. Set up recurring tasks like the Sunday meal plan or the
          weekly schedule, and they just show up done. Your assistant already
          knows your family, so you never start from scratch.
        </p>
      </div>
    </section>
  );
}
